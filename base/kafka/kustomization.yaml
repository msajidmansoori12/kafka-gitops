apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - strimzi-operatorgroup.yaml
  - strimzi-subscription.yaml
  - kafka-cluster.yaml
  - kafka-topic.yaml
  - kafka-node-pool.yaml
  # Demo app: producer + consumer (optional; remove if not needed)
  - demo-app-configmap.yaml
  - demo-producer.yaml
  - demo-consumer.yaml